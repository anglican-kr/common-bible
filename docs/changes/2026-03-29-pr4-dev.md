---
date: 2026-03-29
pr: 4
branch: dev
title: "Dev"
---

# Dev

<!-- CURSOR_SUMMARY -->
> [!NOTE]
> **Medium Risk**
> Large front-end addition plus JSON schema/metadata changes; risk is mainly regressions in routing/search/data loading due to new `app.js` and updated mapping formats.
> 
> **Overview**
> **Shifts the project to a PWA/SPA-first implementation.** Adds a new `app.js` that fetches `data/books.json` and per-chapter JSON to render book/division/chapter/prologue views with hash routing, reading-position persistence, font-size + theme settings, and an in-page audio player with keyboard support.
> 
> **Introduces client-side global search.** The SPA integrates a Web Worker–backed search flow (desktop route + mobile bottom sheet) with result pagination, in-text highlighting, and direct verse-reference navigation.
> 
> **Updates data/docs hygiene.** Adds `data/books.json`, refactors `data/book_mappings.json` (OSIS-style `id`, `testament`, cleaned `aliases_ko`), ignores copyrighted/generated assets (`data/bible/`, `data/search-index.json`), removes legacy `CHANGELOG.md`/`CONTRIBUTING.md`, and rewrites `README.md` plus adds `CLAUDE.md` to reflect the new architecture.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5c73eaf83da5a69a5b94cda658d5682ad0328fa3. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
