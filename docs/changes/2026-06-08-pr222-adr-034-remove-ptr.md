---
date: 2026-06-08
pr: 222
branch: refactor/adr-034-remove-ptr
title: "refactor: 당겨서 새로고침(수동 동기화) 제거 (ADR-034 PR4)"
---

# refactor: 당겨서 새로고침(수동 동기화) 제거 (ADR-034 PR4)

## 무엇을

모바일 화면 최상단에서 아래로 당기면 Drive 북마크 동기화를 **수동 실행**하던 Pull-to-refresh 제스처를 폐기합니다. 사용자 요청(2026-06-08).

- `js/app/views-routing.js` — `setupPullToRefresh` IIFE + 섹션 주석 제거 (2,195 → 2,011줄)
- `css/style.css` — `#pull-refresh-scrim` / `#pull-refresh-indicator` / `.ptr-*` / `@keyframes ptr-spin` 블록 제거
- 인디케이터·스크림은 JS 동적 생성이라 정적 마크업·기타 참조 없음 (잔여 참조 0 확인)

## 백그라운드 동기화는 그대로 유지 (중요)

제거하는 건 **수동 트리거 제스처뿐**입니다. Drive 동기화 자체는 살아있습니다:

- `driveSync.requestSync()` API·상태기계 — 유지 (PTR 외 경로에서도 호출)
- 탭 복귀 시 자동 동기화 (`js/app.js` `visibilitychange`) — 유지
- 로컬 북마크 편집 시 자동 업로드 (debounce) — 유지

즉 사용자는 탭만 다시 봐도 동기화되므로 기능 손실은 "수동으로 당겨서 즉시 동기화" 단축 경로뿐입니다.

## 검증

- ✅ `tsc` main·worker — 0 error
- ✅ 유닛 `node --test` — 674/674
- ✅ e2e `test_navigation.py` + `test_audio.py` — 18/18 (앱 부팅·라우팅·오디오 정상)
- 잔여 `pull-refresh`/`ptr-*` 참조 0 (js·css·html)

## 메모

- ADR-034 PR4를 "추출(`pull-to-refresh.js`)"에서 "제거(폐기)"로 개정.
- **스택 PR** — base가 #221(`refactor/adr-034-audio-split`). #221 머지 후 자동으로 main 대상 재지정됨.
- 1Password 서명 불가로 커밋 미서명(브랜치 보호는 서명 미요구).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> 수동 제스처·스타일만 제거하고 동기화 API·상태기계는 유지해 기능 회귀 범위가 좁습니다.
> 
> **Overview**
> **ADR-034 PR4**에서 모바일 최상단 **당겨서 새로고침** 제스처를 제거합니다. 이전에는 이 동작이 Google Drive 북마크 **수동 동기화**(`driveSync.requestSync`)를 트리거했습니다.
> 
> `views-routing.js`의 `setupPullToRefresh` IIFE와 동적 `#pull-refresh-scrim` / `#pull-refresh-indicator` UI가 삭제되었고, `style.css`의 `.ptr-*` 스타일도 함께 제거됩니다. 문서(`ADR-034`, `architecture.md`, `CLAUDE.md`)는 PR4를 모듈 추출이 아닌 **폐기**로 반영합니다.
> 
> **백그라운드 동기화**(`requestSync`, 탭 복귀 `visibilitychange`, 편집 후 자동 업로드)는 그대로이며, 사용자가 잃는 것은 당겨서 즉시 동기화하는 단축 경로뿐입니다.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 70344ef4c1f38b5c5cc75eb1355ce8c90d9bcc77. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
