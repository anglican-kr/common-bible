---
date: 2026-06-09
pr: 254
branch: release/1.6.1
title: "chore: 1.6.1 릴리스"
---

# chore: 1.6.1 릴리스

## 릴리스 노트 초안 (일상 언어)

- 성경 본문에서 특정 책만 골라 검색하는 기능이 다시 제대로 동작합니다. 검색창에 `사랑 in:요한`처럼 입력하면 요한의 복음서 안에서만 "사랑"을 찾아 줍니다.
- 성서 목록을 위로 넘길 때 위쪽 탭과 책 목록 사이 간격이 한결 자연스러워졌습니다.
- 검색 화면을 단순하게 다듬었습니다. (검색 결과를 한 번 더 좁히던 '좁히기' 입력은 작은 화면에서 불편한 점이 있어 이번에 내렸습니다.)

---

## 변경 사항 (changelog 원본)

### 앱
- fix: SHELL_FILES에 refresh-store.js 추가 + 검증 테스트 (@0x6a6f73687561)
- fix: CSP style-src 해시를 현재 인라인 <style> 블록에 맞게 갱신 (@0x6a6f73687561)
- docs: ADR-017 개정 — njs body 가드 제거 (njs 0.8.2 세그폴트) (@0x6a6f73687561)
- chore: CSP 인라인 해시 자동 검사 도구 추가 + 죽은 script-src 해시 정리 (@0x6a6f73687561)
- style: 성서 목록 탭 아래 고정 여백 + 스크롤 elevation 통일 (@0x6a6f73687561)
- fix: 검색 in:&lt;별칭&gt; 연산자가 책 필터 칩으로 흡수되지 않던 회귀 수정 (@0x6a6f73687561)
- refactor: 검색 '결과 내 검색'(AND 좁히기) 기능 제거 (@0x6a6f73687561)

**Full Changelog**: https://github.com/anglican-kr/common-bible/compare/1.6.0...release/1.6.1

---

버전: 1.6.0 → 1.6.1 (`version.json` · `sw-version.js`). 머지 후 `1.6.1` 태그 + GitHub Release → `deploy.sh`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Search behavior and shareable URLs change (no `and=`); book picker and `in:` scope remain, with moderate surface area across routing, worker, and tab bar clears.
> 
> **Overview**
> **1.6.1** release that drops **“결과 내 검색” (AND narrow)** end-to-end while keeping book-scope search (`in=` / picker chips).
> 
> The **“＋ 좁히기”** in-field UI (`.token-refine-add` / `.token-refine-input`) and related CSS are removed. Search state and URLs no longer use **`and=`** — `parsePath`, `buildSearchUrl`, pagination, `navigateSearch`, and clear actions only carry **`q`**, **`in`**, and **`page`**. The search worker no longer AND-filters verse text on extra terms; `doSearch` only forwards **`scopeBooks`**. **ADR-033** and **`docs/status.md`** record the removal; legacy `and=` in links is ignored. Types and unit tests are updated; **`version.json`** / **`sw-version.js`** go **1.6.0 → 1.6.1**.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 1de0c06f1e7067118dc648ceb703c20c1cae35b7. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
