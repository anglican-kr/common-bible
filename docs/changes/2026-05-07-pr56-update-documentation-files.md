---
date: 2026-05-07
pr: 56
branch: claude/update-documentation-files-0pbkM
title: "docs: CLAUDE.md·README.md Phase 2h 단계 3 머지 시점 갱신"
---

# docs: CLAUDE.md·README.md Phase 2h 단계 3 머지 시점 갱신

## 요약

PR #54(Phase 2h 단계 3) 머지로 발생한 코드/관리 이력 변화를 문서에 반영.

### CLAUDE.md
- **Phase 2h 단계 3**: `🟡 PR open` → `✅ 머지 (2026-05-07, PR #54)`. `_attemptSilentRefresh` + `acceptRedirectCode`, drive-sync IIFE PKCE 콜백 흡수, MAX_REAUTH cap 만성 401 무한 루프 가드, Bugbot 3차 race·URL leak·DISABLED 가드 정정 명시
- **단계 4·5 분리**: 4 = GIS 제거 + PKCE redirect 일원화, 5 = INITIALIZING/IDENTIFYING 상태 제거 + 정리 (예정)
- **유닛 테스트**: `state-machine.test.js` 단독 → `state-machine` (33+) + `refresh-store` (13, Phase 2h 단계 1) + `transport-pkce` (23, Phase 2h 단계 2). 실행 명령을 `tests/unit/*.test.js` 글로브로 갱신
- **프로젝트 구조**: `tests/unit/`에 신규 테스트 파일 2개 추가, `docs/` 트리에 `design/`·`qa/` 디렉터리 추가 표기

### README.md
- **Phase 2h 진행 상황 안내**: Drive 동기화 섹션에 Authorization Code + PKCE + refresh token 마이그레이션 단계 1~3 머지 사실, 단계 4 머지 시점 사용자 영향(cold start 1회 NEEDS_CONSENT) 안내. `docs/design/pkce-migration.md` 링크
- **ADR 범위**: `ADR-001~011` → `ADR-001~013` 정정 (012 TypeScript, 013 유닛 테스트)
- **테스트 명령**: `node --test tests/unit/state-machine.test.js` → `node --test tests/unit/*.test.js`, Node 24+ 표기
- **프로젝트 구조**: `js/sync/` 하위 모듈 5종 명시, `tests/unit/` 테스트 파일 3종 명시, `docs/`에 `design/`·`audit/`·`qa/` 추가

## Test plan

- [x] CLAUDE.md / README.md만 변경 (코드 변경 없음)
- [x] 모든 참조 경로 실제 파일 존재 확인 (`js/sync/refresh-store.js`, `tests/unit/{refresh-store,transport-pkce}.test.js`, `docs/design/pkce-migration.md`, `docs/qa/`)
- [x] PR #54 머지 사실은 GitHub API로 확인 (merged_at 2026-05-07)

---
_Generated by [Claude Code](https://claude.ai/code/session_01QMmXWnK6jB4oEGu9PkwX9K)_

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Documentation-only updates reflecting the merged Phase 2h PKCE/refresh-token work and updated unit-test/CI invocation; no runtime code paths change.
> 
> **Overview**
> Documents the merge of **Phase 2h step 3 (PR #54)**, updating the PKCE migration status/roadmap (including the split of planned steps 4–5) and linking to `docs/design/pkce-migration.md`.
> 
> Updates developer guidance to reflect expanded unit test coverage (`state-machine`, `refresh-store`, `transport-pkce`) and standardizes commands/CI to run `node --test tests/unit/*.test.js` (Node 24+). Also refreshes the documented project tree to include new `docs/design`/`docs/qa` directories and ADR range `001~013`.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 18972ec05f76ed9d080a895cc2804dac2f4fa4d1. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
