---
date: 2026-06-06
pr: 205
branch: feat/verse-action-bar
title: "feat: 절 선택 액션 바 dock 개편 + 분절 산문 절 북마크 통합"
---

# feat: 절 선택 액션 바 dock 개편 + 분절 산문 절 북마크 통합

## 요약

절 선택 흐름 두 건:

1. **절 선택 액션 바 dock 개편** (기존 커밋 `c7cbfac`) — `#verse-select-bar` 를 텍스트 라벨 버튼에서 탭 dock 형식(글래스 pill + 취소 원형, 선택 카운트 부유 칩)으로 개편 + 노트 슬롯 예약. ADR-010 §절 선택 바 / ADR-030 적용 확장.

2. **인용 분절 산문 절 북마크 통합** (이번 작업) — 인용(`<cite>`)이 박힌 산문 절은 인용 위치에서 `23a`/`23b`/`23c` 라인-스팬으로 분절되는데(ADR-022), 절 일부만 선택해 북마크하면 spec 이 `23a,23c` 처럼 분절 저장됐다. 신규 `collapseSegmentedVerses()` 로 **북마크 저장 시에만** 다중-스팬 절을 절 전체(`23`)로 승격.

## 분절 통합 상세

- `js/app/bookmark.js`: `collapseSegmentedVerses(refs, article)` 추가 — 다중-스팬 절이면 `allSelected` 조건 없이 정수 절번호로 통합. `openSaveModal(mode==="verses")` 경로에만 적용.
- 선택 바 라벨(`updateVerseSelectBar`)·복사 직렬화(`copySelectedVerses`)는 종전대로 per-span 단위 유지 — **저장된 북마크만** 통합.
- 운문 다중-스팬 절은 이미 atomic 선택이라 영향 없음.

## 테스트

- `tests/unit/bookmark.test.js`: 6 케이스 추가 (부분 선택 승격 / 단일 스팬 승격 / 전체 선택 패리티 / 단일-스팬 무변경 / 혼합·dedup / null article)
- `node --test tests/unit/*.test.js` → **614 통과**

https://claude.ai/code/session_01WsXJqvu7A6iht28VuVeR39

---
_Generated by [Claude Code](https://claude.ai/code/session_01WsXJqvu7A6iht28VuVeR39)_

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> 주로 북마크 UI·저장 spec 정규화와 CSS 특이도 수정이며, 기존 ID와 테스트로 회귀 범위가 좁다.
> 
> **Overview**
> **절 선택 하단 바**를 탭 dock과 같은 **floating glass** 레이아웃(아이콘 pill + 취소 원형, 위쪽 선택 카운트 칩)으로 바꾸고, 북마크·복사에 **노트 placeholder** 슬롯을 추가한다. 절 선택 중에는 미니 오디오가 tabbar 축소 규칙에 밀려 보이던 문제를 `display: none !important`로 막는다.
> 
> **북마크 저장**만 인용으로 쪼개진 산문 절(`23a`/`23b` 등)을 **절 전체(`23`)**로 올리도록 `collapseSegmentedVerses()`를 도입하고 `openSaveModal("verses")`에 연결한다. 선택 바 문구·복사는 기존처럼 span 단위를 유지한다.
> 
> ADR-010·ADR-030·`CLAUDE.md`에 위 동작을 반영하고, `bookmark.test.js`에 승격 로직 **6건**을 추가한다.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit cf8f0e74a94f7dc53d99fe907e2b32efe73ab208. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
