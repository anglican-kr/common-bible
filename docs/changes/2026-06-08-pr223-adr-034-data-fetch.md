---
date: 2026-06-08
pr: 223
branch: refactor/adr-034-data-fetch
title: "refactor: 데이터 페칭 모듈 분리 data-fetch.js (ADR-034 PR2)"
---

# refactor: 데이터 페칭 모듈 분리 data-fetch.js (ADR-034 PR2)

## 무엇을

ADR-034 모듈 분할 PR2. `views-routing.js`의 데이터 페칭 영역을 leaf 모듈로 추출합니다.

- `js/app/data-fetch.js` (신규, ~85줄) — `loadBooks` / `loadVersion` / `loadChapter` / `loadPrologue` + `booksCache` / `appVersion` 캐시 + `DATA_DIR`. 의존 0(fetch만 사용), DOM·앱모듈 의존 없음 — 분할의 최하위 계층.
- `views-routing.js` **2,011 → 1,942줄**

## 결합 처리 (ADR-034 원칙)

- **내부 호출**(route / renderChapter / renderPrologue)은 `loadBooks` / `loadChapter` / `loadPrologue`를 **명시 ESM import**로 — 전역 facade 경유 제거.
- **외부 소비자**용 `window.{loadBooks,loadVersion,loadChapter,loadPrologue,getBooksCache}` + `window.appVersion` 미러는 data-fetch.js가 소유 (search·app.js·bookmark·settings가 bare global로 호출 — 그 모듈 손댈 때 점진 import 전환).

## 테스트 마커 이동

`DATA_FETCHING` 마커 블록을 새 파일로 옮기고, `views-routing.test.js`의 `extractBlock(name, source = VIEWS_SOURCE)`를 일반화해 `data-fetch.js`를 슬라이스하도록 갱신. DATA_FETCHING 테스트 케이스 자체는 무변경.

## 검증

- ✅ `tsc` main·worker — 0 error
- ✅ 유닛 `node --test` — **674/674** (DATA_FETCHING 11건 포함, 새 파일에서 슬라이스)
- ✅ e2e `test_navigation` + `test_audio` + `test_search` — **30/30** (책 목록·장 로드·검색 데이터 경로 정상)

## ADR-034 진행

PR1(오디오)·PR4(PTR 제거)·**PR2(데이터 페칭)** 완료. 남은 PR: PR3(탭 인디케이터→tabbar)·PR5(라우팅).

> 1Password 서명 불가로 커밋 미서명(브랜치 보호는 서명 미요구).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Behavior-preserving refactor with unchanged window APIs and existing unit/e2e coverage; main risk is script load order or offline shell precache omitting the new file.
> 
> **Overview**
> **ADR-034 PR2** moves Bible JSON loading out of `views-routing.js` into a new leaf module **`js/app/data-fetch.js`** (`loadBooks` / `loadVersion` / `loadChapter` / `loadPrologue`, plus `booksCache` / `appVersion`).
> 
> `views-routing.js` now **ESM-imports** the loaders it uses for routing/rendering and drops duplicate `window` exports; **`data-fetch.js` owns** the existing `window.*` facades so `search.js`, `app.js`, `bookmark.js`, and settings keep working unchanged. **`index.html`** and **`sw.js` SHELL_FILES** register the new script; **`views-routing.test.js`** slices the moved `DATA_FETCHING` marker from `data-fetch.js`. Docs (`CLAUDE.md`, `architecture.md`, ADR-034) note PR2 complete and ~70 fewer lines in `views-routing.js`.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 3547421d45b5458c14b2687e6d4fa3d812c951f6. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
