---
date: 2026-05-07
pr: 58
branch: claude/pkce-step5-cleanup
title: "chore: PKCE 마이그레이션 정리 (Phase 2h 단계 5)"
---

# chore: PKCE 마이그레이션 정리 (Phase 2h 단계 5)

## 요약

Phase 2h PKCE 마이그레이션을 마무리하는 정리 단계. **코드 기능 변경 없음** (한 줄 cleanup 호출 + 문서 갱신). 단계 1~4 머지 후의 살아있는 부채 정리.

## 변경 범위

### 코드

- **`js/drive-sync.js`**: 모듈 로드 시 `localStorage.removeItem("bible-drive-silent-blocked")` 한 줄 추가.
  - 단계 4에서 키를 read/write하는 코드는 모두 사라졌으나 기존 사용자 디바이스에 stale value가 남아있음.
  - `removeItem`은 missing key에 대해 no-op이므로 안전.
  - 몇 릴리스 후(≥6주) cleanup 호출 자체도 제거 예정.

### 문서

- **`docs/coding-pitfalls.md`** — Phase 2h에서 학습한 함정 3개 추가:
  - §11 비동기 race 가드 — 단일 체크포인트의 함정 (Bugbot PR #54·#57)
  - §12 콜백 URL 데이터 leak — flow별 transport 격차 (Bugbot PR #54-2)
  - §13 마이그레이션 시점의 sessionStorage / localStorage 키 격리 (단계 2~4 키 분리·인계 패턴)
- **`docs/audit/2026-05-07-pkce-refresh-token.md`** — 보안 감사 (신규).
  - Critical/High/Medium **0건**.
  - 위협 모델 매트릭스: XSS / 외부 스크립트 / IDB 덤프 / MITM / token 도용 / replay / CSRF / URL leak / 무한 redirect / race window — 전부 ✅.
  - 모니터링 권고 4건: e2e 정기 실행, Cloud Console URI 검증, OAuth 검수 모니터링, silent-blocked cleanup 코드 제거 시점.
- **`README.md`** — Phase 2g 시점의 GIS/Implicit/silent-blocked 표·설명을 Phase 2h 단일 PKCE 경로 표로 교체. 알려진 한계 갱신 (refresh token 7일 만료, 외부 권한 회수 동작 등).
- **`docs/decisions/011-bookmark-sync.md`** — 단계 4·5 완료 마크. Bugbot 단계 4 검토 결과(`f090c83` 추가 commit + 회귀 테스트 26a/b/c) 기록.
- **`docs/design/pkce-migration.md`** — 단계 5 결과 섹션 추가. 단계 1~4 모두 머지 완료 상태로 마크.
- **`CLAUDE.md`** — Phase 2h 진행 중 → 완료. 보안 감사 두 건(Implicit/PKCE 시점) 모두 참조.

## 머지 가능 조건

- [x] `node --test tests/unit/*.test.js` 65/65 통과 (코드 변경 없으므로 단계 4 시점 그대로)
- [x] `npx tsc -p tsconfig.json --noEmit --ignoreDeprecations 6.0` 0 error
- [x] `npx tsc -p tsconfig.worker.json --noEmit --ignoreDeprecations 6.0` 0 error
- [x] silent-blocked 키 잔존 참조 전수 조사 — `js/`, `tests/unit/`에 없음 (docstring 1곳만 — 단계 4 변경 설명)

## Test plan

- [x] tsc 두 config 모두 0 error
- [x] unit 테스트 65/65 통과
- [ ] 로컬 수동 검증: 기존 사용자 시나리오 — `localStorage.setItem("bible-drive-silent-blocked", "1")` 후 앱 재로드 → 키가 자동으로 사라지는지 확인
- [ ] e2e 회귀: `pytest tests/e2e/test_drive_sync*.py` (단계 4 PR과 동일, 코드 영향 없음)

https://claude.ai/code/session_01EGspKZS5UJkTYJNnmC9r2K

---
_Generated by [Claude Code](https://claude.ai/code/session_01EGspKZS5UJkTYJNnmC9r2K)_

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> 실제 런타임 변경은 `js/drive-sync.js`의 localStorage 키 정리 1줄뿐이며 나머지는 문서 갱신이라 위험이 낮습니다. 다만 동기화 관련 코드 경로에서 매 로드마다 실행되므로, 매우 구형 브라우저/프라이빗 모드의 localStorage 예외 여부만 가볍게 확인하면 됩니다.
> 
> **Overview**
> **Phase 2h PKCE 마이그레이션(단계 5) 정리 PR**로, 기능 변경 없이 잔존 부채와 문서를 정돈합니다.
> 
> 코드에서는 `js/drive-sync.js` 로드 시점에 더 이상 사용되지 않는 `bible-drive-silent-blocked` localStorage 키를 `removeItem`으로 *one-shot cleanup*합니다.
> 
> 문서에서는 `README.md`를 PKCE + refresh token 단일 인증 경로 기준으로 갱신하고, `docs/audit/2026-05-07-pkce-refresh-token.md` 보안 감사 보고서 및 `docs/coding-pitfalls.md`에 PKCE 마이그레이션 중 학습한 race/leak/storage-key 격리 주의사항을 추가하며, 관련 설계/ADR/로그 문서에서 단계 4·5 상태를 업데이트합니다.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit a50cf35acf8dd108208363d1311f28d7cf012c37. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
